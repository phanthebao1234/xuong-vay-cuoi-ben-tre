/**
 * Expected RentalCategory slugs for category-pinned routes. Category taxonomy is
 * CMS-driven (FOXIE Admin) — these are routing expectations, not hardcoded production
 * data. Resolved against the live categories list at request time; absent today →
 * elegant EmptyState, never invented.
 */
export const SUIT_CATEGORY_SLUG = 'vest'
export const AO_DAI_CATEGORY_SLUG = 'ao-dai'
